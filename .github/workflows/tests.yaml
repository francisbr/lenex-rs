name: Tests

on:
    pull_request:
        branches:
            - master

jobs:
    cargo-test:
        name: Cargo tests
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - uses: actions-rs/cargo@v1
              with:
                command: test
                args: --workspace